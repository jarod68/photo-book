{
  "name": "photo-book",
  "version": "1.0.0",
  "description": "360° photo viewer",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "node scripts/build.js",
    "docker:build": "npm run test:coverage && npm run build && docker buildx build --platform linux/amd64,linux/arm64 --provenance=mode=max --sbom=true --push -t jarod68/photo-book:latest .",
    "docker:deploy": "npm run docker:build",
    "docker:run": "docker compose up -d",
    "docker:stop": "docker compose down",
    "docker:logs": "docker compose logs -f",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:robot": "bash scripts/test-robot.sh",
    "test:visual": "bash scripts/test-visual.sh",
    "test:visual:update": "bash scripts/test-visual.sh --update-snapshots"
  },
  "dependencies": {
    "archiver": "^8.0.0",
    "bcryptjs": "^2.4.3",
    "cookie-parser": "^1.4.7",
    "exifr": "^7.1.3",
    "express": "^4.18.2",
    "express-rate-limit": "^8.5.2",
    "helmet": "^8.1.0",
    "multer": "^2.1.1",
    "pg": "^8.13.3",
    "sharp": "^0.34.5"
  },
  "overrides": {
    "chokidar": "^4.0.3"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@vitest/coverage-v8": "^4.1.5",
    "esbuild": "^0.28.0",
    "happy-dom": "^20.9.0",
    "nodemon": "^3.0.2",
    "supertest": "^7.2.2",
    "vitest": "^4.1.5"
  }
}
