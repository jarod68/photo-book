# Local override — bypasses Traefik, exposes app directly on port 3000.
# Usage: docker compose -f docker-compose.yml -f docker-compose.local.yml up
services:
  traefik:
    profiles: [disabled]

  photo-book:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=test
